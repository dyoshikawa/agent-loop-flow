name: Publish Assets

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g., v7.6.2)"
        required: true
        type: string

permissions:
  contents: write # Required for uploading release assets

jobs:
  build-assets:
    name: Build and upload assets
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          ref: main

      - name: Setup mise
        uses: jdx/mise-action@1648a7812b9aeae629881980618f079932869151 # v2
        with:
          experimental: true

      - name: Install dependencies
        run: bun install

      - name: Build binaries
        run: |
          mkdir -p dist-bun
          bun build --compile --minify --sourcemap --target=bun-linux-x64 --outfile=dist-bun/agent-loop-flow-linux-x64 ./src/cli/index.ts
          bun build --compile --minify --sourcemap --target=bun-linux-arm64 --outfile=dist-bun/agent-loop-flow-linux-arm64 ./src/cli/index.ts
          bun build --compile --minify --sourcemap --target=bun-darwin-arm64 --outfile=dist-bun/agent-loop-flow-darwin-arm64 ./src/cli/index.ts
          bun build --compile --minify --sourcemap --target=bun-darwin-x64 --outfile=dist-bun/agent-loop-flow-darwin-x64 ./src/cli/index.ts
          bun build --compile --minify --sourcemap --target=bun-windows-x64 --outfile=dist-bun/agent-loop-flow-windows-x64.exe ./src/cli/index.ts

      - name: Generate checksums
        run: |
          cd dist-bun
          sha256sum agent-loop-flow-* > SHA256SUMS
          cat SHA256SUMS

      - name: Generate repomix TOON variants
        run: bun scripts/generate-repomix-toon.ts

      - name: Bundle TOON files into zip
        run: zip repomix-toon.zip repomix-output-*.toon

      - name: Determine tag name
        id: tag
        run: |
          if [ -n "$TAG_INPUT" ]; then
            echo "tag=$TAG_INPUT" >> "$GITHUB_OUTPUT"
          else
            echo "tag=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          fi
        env:
          TAG_INPUT: ${{ inputs.tag }}

      - name: Upload assets to release
        uses: softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe # v2.6.1
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          draft: true
          files: |
            install.sh
            dist-bun/agent-loop-flow-linux-x64
            dist-bun/agent-loop-flow-linux-arm64
            dist-bun/agent-loop-flow-darwin-arm64
            dist-bun/agent-loop-flow-darwin-x64
            dist-bun/agent-loop-flow-windows-x64.exe
            dist-bun/SHA256SUMS
            repomix-toon.zip
