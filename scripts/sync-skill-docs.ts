// oxlint-disable no-console

/**
 * This script was used to sync VitePress docs to skill files.
 * It is currently disabled as the docs/ directory has been removed
 * during the agent-loop-flow migration.
 */

function main(): void {
  console.log("sync-skill-docs: No docs to sync (agent-loop-flow migration)");
}

main();
